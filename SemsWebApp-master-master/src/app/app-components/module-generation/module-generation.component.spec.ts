import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ModuleGenerationComponent } from './module-generation.component';

describe('ModuleGenerationComponent', () => {
  let component: ModuleGenerationComponent;

  beforeEach(async () => {
  });

  beforeEach(() => {
  });

  it('should create', () => {
  });
});
