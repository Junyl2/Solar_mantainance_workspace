import { ComponentFixture, TestBed } from '@angular/core/testing';

import { UseRateComponent } from './use-rate.component';

describe('UseRateComponent', () => {
  let component: UseRateComponent;

  beforeEach(async () => {
  });

  beforeEach(() => {
  });

  it('should create', () => {
  });
});
